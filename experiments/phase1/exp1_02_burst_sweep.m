%% exp1_02_burst_sweep.m
% Experiment 1-2: 트래픽 버스트 강도(rho, alpha) 2D 스윕
%
% Research Question: 
%   트래픽이 얼마나 뭉쳐서 오는가(burstiness)가 UORA 경쟁에
%   미치는 영향은?
%
% 스윕 변수 1 (rho): [0.3, 0.5, 0.7, 0.9]
% 스윕 변수 2 (alpha): [1.2, 1.5, 1.8, 2.1]
%
% 고정: L_cell = 0.5

clear; close all; clc;

%% =====================================================================
%  1. 실험 설정
%  =====================================================================

% (이전에 생성한 Exp 1-2 설정 파일을 호출합니다)
exp_config = get_exp1_02_config();

%% =====================================================================
%  2. 실험 실행
%  =====================================================================

% (범용 실행기가 2D 스윕을 알아서 처리합니다)
results_grid = run_sweep_experiment(exp_config);

%% =====================================================================
%  3. 결과 저장
%  =====================================================================

% (범용 저장 함수가 2D 결과를 .mat 및 .csv로 저장합니다)
save_experiment_results(results_grid, exp_config);

%% =====================================================================
%  4. Quick Plot
%  =====================================================================

% (범용 플롯 함수가 2D 결과에 맞춰 Heatmap을 생성합니다)
quick_plot(results_grid, exp_config);

%% =====================================================================
%  5. 완료 메시지
%  =====================================================================

fprintf('========================================\n');
fprintf('  Exp 1-2 (2D 스윕) 완료\n');
fprintf('========================================\n\n');
fprintf('  - Heatmap이 ''results/quick_plots/%s_quick.png''에 저장되었습니다.\n', exp_config.name);
fprintf('  - 상세 .mat 파일이 ''results/mat/''에 저장되었습니다.\n');
fprintf('  - 분석을 위해 ''analyze_exp1_02_burst_sweep.m''을 실행하세요.\n\n');

fprintf('🎉 Experiment 1-2 완료!\n\n');